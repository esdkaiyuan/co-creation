# 前端部署包说�?

## 包含内容
- src/ - 前端源代�?
- .env.production - 生产环境配置

## 快速部�?
3. 配置 Nginx 反向代理 /api/ 到后�?

## Nginx 配置示例
location /api/ {
    proxy_pass http://127.0.0.1:5000/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}

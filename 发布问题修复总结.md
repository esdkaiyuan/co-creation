# 发布项目功能问题修复总结

## 问题描述

用户报告发布项目后出现以下问题:
1. ✅ 弹出"发布成功"提示
2. ❌ 同时也弹出"请求失败"提示
3. ❌ 项目在主页中找不到

## 问题诊断

### 根本原因

**参数名称不匹配**: 
- 前端HomeView使用`search`参数传递搜索关键词
- 后端期望的参数名是`keyword`
- 导致首页获取项目列表时,后端无法正确处理搜索参数

### 次要问题

**Token无效**:
- 浏览器中存储的是测试Token `test-token-12345`
- 不是真实的JWT Token
- 导致上传图片时返回403错误

## 修复方案

### 修复1: 兼容search和keyword参数

**文件**: `server.cjs`

**修改内容**:
```javascript
// 修改前
const { page = 1, pageSize = 12, categoryId, filter, sort, keyword } = req.query;

// 修改后
const { page = 1, pageSize = 12, categoryId, filter, sort, keyword, search } = req.query;
const searchKeyword = search || keyword;  // 兼容两种参数名
```

**影响范围**:
- 项目列表查询接口 `/api/projects`
- 关键词搜索功能
- 计数查询

### 修复2: 改进发布页面错误处理

**文件**: `views/PublishProjectView.vue`

**修改内容**:
```javascript
// 添加响应日志
const res = await projectStore.createNewProject(projectForm)
console.log('发布成功响应:', res)

// 延迟跳转,让用户看到成功提示
setTimeout(() => {
  router.push('/')
}, 500)

// 添加错误提示
catch (error) {
  console.error('发布失败:', error)
  ElMessage.error(error.message || '发布失败,请重试')
}
```

### 修复3: 创建Token修复工具

**文件**: `fix-token.html`

**功能**:
- 一键清除旧Token
- 自动登录获取新JWT Token
- 验证Token有效性
- 引导用户前往发布页面

## 测试结果

### API测试

✅ **发布项目**:
```bash
POST /api/projects
状态码: 200
响应: {"code":200,"message":"项目创建成功","data":{"projectId":21}}
```

✅ **获取项目列表(带search参数)**:
```bash
GET /api/projects?page=1&pageSize=12&search=
状态码: 200
响应code: 200
项目数量: 12
```

### 功能验证

1. ✅ 发布项目成功
2. ✅ 只显示"发布成功"提示,不再显示"请求失败"
3. ✅ 项目出现在首页列表中
4. ✅ 图片上传功能正常(Token修复后)

## 相关文件

### 修改的文件
- `server.cjs` - 兼容search/keyword参数
- `views/PublishProjectView.vue` - 改进错误处理和用户体验

### 新增的文件
- `fix-token.html` - Token修复工具

## 使用说明

### 如果遇到Token 403错误

1. 访问 http://localhost:3000/fix-token.html
2. 点击"🚀 一键修复Token"按钮
3. 等待修复完成
4. 点击"📱 前往发布页面"

### 测试账号

```
邮箱: test@example.com
密码: 123456
```

## 经验总结

1. **前后端参数命名要一致**: 避免使用不同的参数名导致兼容性问题
2. **错误处理要明确**: catch块中应该显示具体的错误信息
3. **用户体验优化**: 成功提示后延迟跳转,让用户有时间看到提示
4. **提供修复工具**: 对于常见问题(如Token失效),提供一键修复工具

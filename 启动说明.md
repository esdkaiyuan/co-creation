# 🚀 共创平台 - 快速启动指南

## 📋 前提条件检查

在启动项目之前,请确保以下条件已满足:

### 1. ✅ Node.js 已安装
检查命令:
```bash
node --version
npm --version
```

如果未安装,请从 https://nodejs.org/ 下载并安装 (推荐 LTS 版本)

### 2. ⚠️ MySQL 服务需要启动

**重要: 需要以管理员身份启动 MySQL 服务!**

#### 方法一: 使用 PowerShell (管理员)
1. 右键点击 PowerShell
2. 选择"以管理员身份运行"
3. 执行命令:
```powershell
net start MySQL
```

#### 方法二: 使用服务管理器
1. 按 `Win + R`
2. 输入 `services.msc` 并回车
3. 找到 "MySQL" 服务
4. 右键点击,选择"启动"

#### 方法三: 使用 MySQL 安装目录
```powershell
# 以管理员身份运行
cd C:\mysql-enterprise\bin
.\mysqld.exe --install
net start MySQL
```

---

## 🎯 一键启动 (推荐)

### 步骤 1: 启动 MySQL 服务
按照上面的说明,以管理员身份启动 MySQL 服务

### 步骤 2: 一键启动前后端
双击运行项目根目录下的:
```
一键启动.bat
```

这个脚本会自动:
- ✅ 检查 Node.js 环境
- ✅ 检查 MySQL 连接
- ✅ 安装依赖 (如果未安装)
- ✅ 启动后端服务 (端口 5000)
- ✅ 启动前端服务 (端口 3000)
- ✅ 自动打开浏览器

---

## 🔧 手动启动

### 启动后端服务

```bash
# 方式 1: 使用启动脚本
start-backend.bat

# 方式 2: 手动启动
node server.cjs
```

后端将在 `http://localhost:5000` 启动

### 启动前端服务

打开新的终端窗口:

```bash
# 方式 1: 使用启动脚本
start-frontend.bat

# 方式 2: 手动启动
npm run dev
```

前端将在 `http://localhost:3000` 启动

---

## 🧪 测试系统

### 1. 测试后端是否运行

浏览器访问: http://localhost:5000/api/health

应该看到:
```json
{
  "code": 200,
  "message": "服务正常",
  "data": {
    "status": "ok",
    "timestamp": "2026-05-06T..."
  }
}
```

### 2. 测试前端是否运行

浏览器访问: http://localhost:3000

应该看到共创平台的首页

### 3. 测试账号登录

**测试账号:**
- 邮箱: `test@example.com`
- 密码: `123456`

**其他账号 (密码都是 password123):**
- zhangsan@example.com (张三)
- lisi@example.com (李四)
- wangwu@example.com (王五)
- zhao liu@example.com (赵六)
- qianqi@example.com (钱七)
- sunba@example.com (孙八)
- zhoujiu@example.com (周九)
- wushi@example.com (吴十)

---

## 📊 数据库信息

### 连接信息
```
主机: localhost
端口: 3306
数据库: co_creation_esdk
用户名: co_creation_esdk
密码: GchzPPQ8sM6Rc2Xn
```

### 数据库包含的内容
- ✅ 9 个用户 (包含测试账号)
- ✅ 8 个分类 (技术开发、设计创意等)
- ✅ 8 个项目 (带完整信息)
- ✅ 8 条参与记录
- ✅ 8 条点赞记录

### 查看数据库
```bash
"C:\mysql-enterprise\bin\mysql.exe" -u co_creation_esdk -pGchzPPQ8sM6Rc2Xn co_creation_esdk
```

### 重置数据库
如果需要重新初始化数据库:
```bash
"C:\mysql-enterprise\bin\mysql.exe" -u root co_creation_esdk < init_database.sql
```

---

## 🔍 常见问题

### Q1: 启动后端时出现 "ECONNREFUSED" 错误

**原因**: MySQL 服务未启动

**解决方法**:
```powershell
# 以管理员身份运行
net start MySQL
```

### Q2: "net start MySQL" 提示 "拒绝访问"

**原因**: 需要管理员权限

**解决方法**:
1. 右键点击 PowerShell 或命令提示符
2. 选择"以管理员身份运行"
3. 再次执行 `net start MySQL`

### Q3: 前端无法连接后端

**检查项**:
1. 后端服务是否正常运行在 5000 端口
2. 访问 http://localhost:5000/api/health 测试后端
3. 检查 `vite.config.js` 中的代理配置

**解决方法**:
确保后端先启动,然后再启动前端

### Q4: 登录失败

**检查项**:
1. MySQL 服务是否运行
2. 数据库是否正确初始化
3. 使用的账号密码是否正确

**测试数据库连接**:
```bash
"C:\mysql-enterprise\bin\mysql.exe" -u co_creation_esdk -pGchzPPQ8sM6Rc2Xn co_creation_esdk -e "SELECT COUNT(*) FROM users;"
```

### Q5: 端口被占用

**查看端口占用**:
```bash
netstat -ano | findstr :5000  # 后端
netstat -ano | findstr :3000  # 前端
```

**解决方法**:
- 关闭占用端口的程序
- 或修改配置文件使用其他端口

---

## 📁 项目结构

```
co-creation/
├── 一键启动.bat              # ⭐ 一键启动前后端
├── start-backend.bat         # 启动后端服务
├── start-frontend.bat        # 启动前端服务
│
├── server.cjs                # 后端服务入口
├── package.json              # 前端依赖配置
├── package.backend.json      # 后端依赖配置
├── vite.config.js            # Vite 配置
│
├── .env.production           # 前端环境配置
├── .env.example              # 后端环境配置示例
│
├── init_database.sql         # 数据库初始化脚本
├── create_database.sql       # 创建数据库脚本
│
├── README.md                 # 项目说明
├── README_DEPLOY.md          # 部署说明
├── QUICK_START.md            # 快速启动指南
├── 启动说明.md               # 本文件
└── COMPLETION_REPORT.md      # 完成报告
│
├── api/                      # 前端 API 调用
├── views/                    # 页面组件
├── components/               # 公共组件
├── store/                    # 状态管理
├── router/                   # 路由配置
├── utils/                    # 工具函数
└── assets/                   # 静态资源
```

---

## 🎯 快速操作指南

### 第一次启动
1. 以管理员身份启动 MySQL: `net start MySQL`
2. 双击 `一键启动.bat`
3. 等待浏览器自动打开
4. 使用测试账号登录: `test@example.com` / `123456`

### 日常开发
1. 确保 MySQL 已启动
2. 双击 `一键启动.bat`
3. 开始开发!

### 停止服务
- **前端**: 在终端按 `Ctrl + C`
- **后端**: 关闭后端窗口,或在终端按 `Ctrl + C`

---

## 📚 更多信息

- 数据库详细文档: `DATABASE_README.md`
- 部署说明: `README_DEPLOY.md`
- 快速指南: `QUICK_START.md`
- 完成报告: `COMPLETION_REPORT.md`

---

**祝使用愉快! 🎉**

如有问题,请查看上方的常见问题部分或相关文档。
